rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow global read and write access to all collections and subcollections
    // in the dev/preview environment to guarantee zero "Missing or insufficient permissions" errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
